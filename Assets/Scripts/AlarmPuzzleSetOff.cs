using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AlarmPuzzleSetOff : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

// When the alarm puzzle is set off, wait x seconds, move all creatures within r radius to alarm