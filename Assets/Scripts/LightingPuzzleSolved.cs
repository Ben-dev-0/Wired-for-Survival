using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LightingPuzzleSolved : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

// This is basically just a placeholder but what you will use is NameOfLightGameObject.enabled = false or = true to turn the light on or off.